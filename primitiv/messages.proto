syntax = "proto3";

package primitiv.messages;

message Shape {
  repeated uint32 dims = 1;
  uint32 batch = 2;
}

message Tensor {
  Shape shape = 1;
  repeated float data = 2;
}

message Parameter {
  Tensor value = 1;
  map<string, Tensor> stats = 2;
}
